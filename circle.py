import math 

radius = int(input("what is the radius of the circle?  "))
pi = 3.14
diameter = 2 * radius
circumference = 2 * pi * radius
area = pi * radius **2
print()
print(f"if a circle has a radius of {radius} unit the diameter will be {diameter} unit with a circumference of {circumference} unit, and an area of {area} unit.")
